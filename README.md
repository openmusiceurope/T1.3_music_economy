# T1.3_music_economy
R code for producing tables on the music sector (employment, enterprises, and related indicators) using Eurostat data and eurostat R package. The tables can be generated running `eurostat_tables.R` file. The `music_employment.csv` is needed to calculate the tables.
